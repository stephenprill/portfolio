<div class="main">
  <div class="project-title">
    Welcome To The Projects
  </div>

    {{#each project in model}}
    <div class="job">
      <h3>{{#link-to 'project' project}}
        {{project.name}}
        {{/link-to}}
      </h3>
      <h4>{{project.description}}</h4>
    {{/each}}
    </div>




</div>


    {{outlet}}
